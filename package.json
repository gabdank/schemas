{
  "name": "database-schemas",
  "version": "1.0.0",
  "description": "JSON Schema definitions for biological database entities",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "validate": "echo 'Running Jest tests for comprehensive schema validation' && npm test",
    "validate:schemas": "echo 'Schema validation handled by Jest tests due to custom mixinProperties extension'",
    "validate:examples": "echo 'Example validation handled by Jest tests due to custom mixinProperties extension'",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gabdank/schemas.git"
  },
  "keywords": [
    "json-schema",
    "database",
    "biological-data",
    "validation"
  ],
  "author": "gabdank",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/gabdank/schemas/issues"
  },
  "homepage": "https://github.com/gabdank/schemas#readme",
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10"
  }
}
